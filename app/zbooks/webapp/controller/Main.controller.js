sap.ui.define([
    "sap/ui/core/mvc/Controller"
], function (Controller) {
    "use strict";

    return Controller.extend("zbooks.controller.Main", {
        onInit: function () {
            // Hàm khởi tạo tự động chạy khi View được load
        }
    });
});